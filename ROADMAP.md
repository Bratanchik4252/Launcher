# ROADMAP лаунчера NOVACRAFT

> Лаунчер Minecraft **1.7.10** (версия проекта сменилась с 1.12.2!). Electron + React + TypeScript.
> Код лежит в `launcher/`. Этот файл — план и список TODO. Сайт-ROADMAP — в корне `ROADMAP.md`.

**Жёсткие требования (от владельца):**
- Без бэкдоров и дыр, нельзя заддосить, нельзя украсть токены.
- **IP игрового сервера не палится**: не лежит в клиенте/exe, выдаётся из БД, серверный мод проверяет токен.
- Нельзя зайти на сервер с левыми клиентскими модами (серверный мод проверяет мод-лист + токен лаунчера).
- Стиль — как у сайта NOVACRAFT (тёмный glass, зелёные акценты).
- Авторизация — те же аккаунты, что на сайте (Supabase), вход по нику ИЛИ email.
- Сейчас сервер на хостинге (есть защита), позже — свой ПК: тогда поднимать прокси + фаервол.

---

## Фаза 1 — брендинг, авторизация, безопасность (сейчас)

- [x] Скопирован код в `launcher/` (без node_modules/dist).
- [ ] Удалить `api-auth/` и `mock-api/` (свой Node-сервер — лишняя цель; всё через Supabase).
- [ ] Ребрендинг: `package.json` (name/productName/author, версия 0.1.0), `launcher.config.json` (brandName NOVACRAFT), убрать все «!!!!!».
- [ ] Авторизация через **Supabase** (`supabase-js` в main-процессе):
  - вход по нику ИЛИ email + пароль (ник → email через запрос в `profiles`);
  - сессия (JWT) хранится в `safeStorage`/DPAPI, восстановление при старте;
  - **антибрутфорс**: 5 неудачных попыток → блок 5 минут (как на сайте, `mc:attempts`-аналог локально).
- [ ] Удалить `devAuthBypass` и любой вход без пароля.
- [ ] Убрать plaintext-фолбэк сессий (только DPAPI, иначе — не хранить токен вообще).
- [ ] **IP сервера убрать из клиента**: `serverHost/serverPort` больше не в `launcher.config.json`; адрес приходит из таблицы `launcher_meta` (Supabase) при запуске.
- [ ] Запуск игры: передавать игре `--server/--port/--username` (автоподключение к серверу), токен сессии — клиентскому моду (в файл/arg, права 0600).
- [ ] SHA-256 проверка скачанного модпака и Java (хэш из `launcher_meta`/GitHub release asset).
- [ ] Убрать PowerShell-инъекцию при распаковке (native unzip вместо Expand-Archive).
- [ ] Рестайл UI в стиль сайта: тёмная тема, glass-карточки, зелёные акценты, логотип/фавиконка NOVACRAFT.
- [ ] Схема Supabase лаунчера: `launcher_meta`, `hwid_bans` (без service_role: только Edge Function с JWT), белый список модов.
- [ ] Удалить мёртвый код (`SettingsPanel`, `LoginPage`).

## Фаза 2 — модпак и обновления (после Фазы 1)

- [ ] Модпак: GitHub Releases (репозиторий Bratanchik4252/ModPack или новый) + SHA-256 asset.
- [ ] Апдейтер лаунчера: GitHub Releases + версия из package.json, автообновление без заглушек.
- [ ] Java 8: Adoptium JRE + SHA-256 из API (там есть checksum-поле).
- [ ] Для 1.7.10 проверить аргументы запуска и папку библиотек (libraries/*.jar в 1.7.10 лежат иначе).

## Фаза 3 — серверная защита (после Фазы 2)

- [ ] **Серверный Forge-мод 1.7.10 «NovaGuard»**: при входе получает от клиент-мода JWT-токен + список модов;
  проверяет токен через Edge Function `/verify-token` (JWT-валидация, бан-статус, HWID), мод-лист — против белого списка из БД;
  нет токена / не те моды / бан / чужой HWID → кик до завершения входа.
- [ ] **Клиентский мод «NovaGuardClient» 1.7.10**: отправляет токен и мод-лист серверу сразу после входа (SimpleNetworkWrapper).
- [ ] ForgeActionLogger: дописать отправку логов на сервер (сейчас только локальный файл) — по желанию.
- [ ] Хостинг/свой ПК: инструкция по фаерволу (только прокси-IP), позже поднять Velocity/BungeeCord при переходе на свой ПК.

## Фаза 4 — релиз и багфикс

- [ ] Собрать `npm run dist` (NSIS setup), проверить на чистой Windows.
- [ ] Прогнать весь флоу: регистрация на сайте → логин в лаунчере → модпак → вход на сервер.
- [ ] Исправить визуальные баги по отзывам.
- [ ] Загрузить релиз на GitHub Releases (обновления для лаунчера).

---

## Что НЕ переносим из старого кода (найдено при ревизии)

| Проблема | Статус |
|---|---|
| `devAuthBypass` — вход без пароля | удалить |
| Offline-запуск без accessToken/uuid | убрать (ник из аккаунта, автосервер) |
| Модпак без checksum → RCE при подмене | добавить SHA-256 |
| Edge Functions с `SUPABASE_SERVICE_ROLE_KEY` публично | не использовать; JWT + anon |
| `api-auth` с CORS `*`, HTTP, без rate limit | удалить |
| `mock-api` с паролями открытым текстом | удалить |
| PowerShell `Expand-Archive` инъекция | заменить на нативный unzip |
| Токены в plaintext при недоступном safeStorage | не хранить |
| IP сервера в `launcher.config.json` | убрать, адрес из БД |
| Токен ничем не проверяется | проверка через серверный мод + Edge Function |

## Текущий стек

- Electron 37, React 19, TypeScript, Vite 7, electron-builder (NSIS).
- Существующий скелет (окно, IPC, preload с contextIsolation/sandbox, i18n, HWID-сбор) — переиспользуем.
- Авторизация: Supabase (проект сайта). Пароли в безопасном хэше Supabase Auth, rate limit встроенный.
